<section
  id="hero"
  class="hero-section relative flex min-h-[100svh] items-start justify-center overflow-hidden bg-[#e8e2cd] pt-[42vh] text-[#626951]"
>
  <video
    class="hero-video absolute inset-0 z-[1] h-full w-full object-cover object-center"
    src="/reference/majestic-hero-bg.mp4"
    poster="/reference/majestic-hero-poster.jpg"
    autoplay
    muted
    loop
    playsinline
    preload="auto"
    aria-hidden="true"></video>

  <div
    class="relative z-10 flex flex-col items-center px-6 pt-[70px] text-center"
  >
    <p
      class="hero-morph mb-4 font-sans text-sm font-medium uppercase tracking-[0.3em] text-[#626951]/80 md:text-base"
    >
      We are getting married
    </p>

    <h1
      class="hero-morph mb-3 text-6xl font-normal leading-none text-[#596347] md:text-8xl"
      style="font-family: var(--font-family-script), 'Great Vibes', cursive;"
    >
      Tayo &amp; Ope
    </h1>

    <p
      class="hero-morph font-serif text-sm uppercase tracking-[0.2em] text-[#626951]/70 md:text-base"
    >
      December 16 - 20, 2026
    </p>
  </div>
</section>

<style>
  .hero-video {
    filter: saturate(0.94) contrast(0.98) brightness(1.04)
      drop-shadow(0 0 34px rgba(42, 39, 28, 0.18));
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-video {
      display: none;
    }
    .hero-section {
      background:
        radial-gradient(
          circle at 50% 42%,
          rgba(255, 250, 232, 0.92),
          transparent 42%
        ),
        #e8e2cd;
    }
  }
</style>
